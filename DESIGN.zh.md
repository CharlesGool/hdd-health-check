# hdd-health-check — 设计文档

[English](DESIGN.md) | **简体中文**

> 译自 `DESIGN.md`（v1.0.0）。如有冲突，以英文版为准。

## 目标与非目标

**目标：** 让系统管理员用一条命令，把一块或多块机械硬盘的原始 SMART 数据、内核日志、挂载状态
转化为一个通俗易懂的健康结论（0-100 评分 + 等级），既能交互式单次检查，也能配合 cron 无人值守巡检。

**非目标：**
- 不是监控守护进程——这是一次性扫描，不是长期运行的服务。定时调度交给调用方（cron、systemd timer）。
- 不是数据恢复工具——它只报告风险，不做镜像或恢复。
- 不是覆盖所有介质的通用存储健康工具——SSD/NVMe 默认不在扫描范围内（见 `--include-ssd`）；
  多项评分指标（主轴重试、负载循环次数、机械盘温度区间）对固态存储没有意义。
- 刻意排除破坏性操作（写模式 `badblocks`、安全擦除等）——可选的表面扫描只用
  `badblocks -s`（只读模式），绝不使用具有破坏性的 `-w` 写测试模式。

## 架构

单一自包含的 bash 脚本，无守护进程，除了它写入的日志文件外没有其他外部状态。全部逻辑在单进程内同步执行。

```
main()
├── enumerate_disks()      lsblk -> D_NAME[]/D_SIZE[]/D_MODEL[]/D_ROTA[]/D_TRAN[]
├── select_targets()       -a | -d <列表> | 交互菜单 -> TARGETS[]
└── 对每个目标执行 check_disk()
    ├── 1. 基础信息 + 挂载/只读降级检测
    ├── 2. SMART 可用性（自动探测 USB 桥接/RAID 所需的 smartctl -d 参数）
    ├── 3. SMART 总体健康判定（PASSED/FAILED）
    ├── 4. 关键属性解析 + 逐项评分（ATA 路径 或 SAS 缺陷表路径）
    ├── 5. 温度
    ├── 6. 使用寿命与负载（通电时长/次数、磁头负载循环次数）
    ├── 7. SMART 错误日志
    ├── 8. 自检日志（+ 可选发起新的 short/long 自检）
    ├── 9. 内核日志（dmesg）I/O 错误扫描
    ├── 10. 可选：hdparm 顺序读性能测试
    ├── 11. 可选：badblocks 只读表面扫描
    └── 12. 综合评分 -> 等级 -> 写入每盘 SUMMARY[] 条目
-> 打印 SUMMARY[] 汇总表，计算最差退出码，全部写入 LOG_FILE
```

每次 `check_disk()` 都从固定预算（`SCORE=100`）开始，每发现一个具体问题就按权重扣分，
同时把可读的问题描述累积进 `ISSUES[]`。单盘的最终等级基于评分阈值判定，
所有受检磁盘中最差的等级决定进程退出码（0/1/2）。

**为什么用单一扁平脚本而不是模块化工具：** 这个工具的定位是复制到一台机器上，
除了 `apt install smartmontools` 之外零安装步骤即可运行——单文件、无外部代码依赖，
对一个以 root 身份操作磁盘硬件的工具来说更容易审计和信任。

## 技术栈

| 组件 | 选择 | 原因 |
|---|---|---|
| 语言 | Bash（`set -o pipefail`） | 在任何 Debian/Ubuntu 机器上零安装；这是对现有命令行工具的胶水代码，不需要真正的数据结构 |
| SMART 数据 | `smartmontools`（`smartctl`） | Linux 上标准的 SMART 工具；必需依赖，缺失时通过 `apt` 自动安装 |
| 磁盘枚举 | `util-linux`（`lsblk`） | 标准、结构化（`-P` key=value 输出），可通过 `ROTA` 可靠区分机械盘与固态盘 |
| 可选读性能测试 | `hdparm -tT` | 只读、安全；无需写测试文件即可获取顺序读数值的最简方式 |
| 可选表面扫描 | `e2fsprogs`（`badblocks -s`，只读模式） | 只读表面扫描能捕捉到仅靠 SMART 属性可能漏掉的介质缺陷；刻意从不使用具破坏性的 `badblocks -w` |

无数据库、无配置文件格式、无网络调用——一切都是 `lsblk`/`smartctl`/`dmesg` 调用或命令行参数。

## 复现要求

### 环境

- Debian 或 Ubuntu（通过 `/etc/os-release` 检查；其他发行版只给出警告而非硬性失败，
  因为 `smartctl`/`lsblk` 本身与发行版无关——只有 `apt` 自动安装这一步是 Debian 特有的）。
- root 权限（强制检查 `EUID -eq 0`）。
- 至少一块真实块设备可供扫描——SMART/内核日志行为无法在虚拟磁盘或没有设备直通的容器内有意义地验证。

### 外部依赖

- `smartmontools` —— 必需；脚本检测到缺失时会提示 `apt install`。
- `hdparm` —— 仅在使用 `-s/--speed` 时需要。
- `e2fsprogs`（`badblocks`） —— 仅在使用 `-b/--badblocks` 时需要。
- 从干净检出运行本脚本不需要任何 API key、网络服务或版本控制之外的文件。

### 路径与挂载

- `LOG_DIR` / `-l, --log <文件>` —— 唯一与主机相关的路径。默认 `/var/log/disk-health/`，
  始终可通过 `-l` 按次覆盖，脚本其他地方不会硬编码。

### 配置参考

无环境变量配置。路径相关的唯一命令行参数见 `README.md` § 配置 一节（`-l`/`--log`）。

## 从零搭建

1. `git clone https://github.com/CharlesGool/hdd-health-check.git && cd hdd-health-check`
2. `chmod +x hdd-health-check.sh`
3. `sudo ./hdd-health-check.sh -a` —— 缺失时自动安装 `smartmontools`（除非传 `-y` 否则会提示确认），
   扫描所有检测到的机械盘。
4. **验证是否装对：** 确认看到以综合评分+等级收尾的每盘报告、一份汇总表，
   以及与检测到的最差等级一致的退出码（0/1/2；3 表示运行本身失败——查看打印的错误信息）。

## 数据模型 / 文件布局

无数据库或结构化数据文件——唯一的持久化输出是纯文本日志：

```
${LOG_DIR:-/var/log/disk-health}/hdd-health-<YYYYMMDD-HHMMSS>.log
```

内容与终端输出完全一致，但去掉了 ANSI 颜色码（`out()` 同时写终端和日志；
日志副本经过 `sed -r 's/\x1B\[[0-9;]*[mK]//g'` 处理）。目前没有 JSON/CSV 输出格式——见"如何扩展"。

## 已知限制与坑

- **SAS/SCSI 磁盘走不同的评分路径。** 标准 ATA SMART 属性表（重映射扇区、待映射扇区等）在
  SAS/SCSI 盘上不存在；脚本改为解析增长型缺陷表和读/写不可纠正错误计数
  （见 `check_disk()` 中的 SAS 分支）。该路径上的评分权重与 ATA 路径相互独立，
  且实战验证程度不如 ATA 路径充分。
- **USB 桥接/RAID 控制器直通是启发式探测。** `detect_dopt()` 依次尝试一组固定的
  `smartctl -d` 参数（`sat`、`sat,12`、`scsi`、`ata`、若干 USB 桥接芯片名），
  取第一个能返回可解析设备型号的参数。不常见的桥接芯片或 RAID 控制器（如 MegaRAID）
  可能不在这个列表里——探测失败时脚本会打印手动排查命令
  `smartctl -d megaraid,0`，而不是继续瞎猜。
- **温度属性 ID 因厂商而异。** 脚本读取属性 194（Temperature_Celsius），
  失败则回退到 190（Airflow_Temperature_Cel），再失败则解析
  "Current Drive Temperature" 这行自由文本。用其他属性 ID 上报温度的盘会显示为无法读取，
  而不是显示错误数值——这是"失败即安全"的设计，但覆盖面并非详尽无遗。
- **评分权重是经验判断，不是基于故障率模型推导的。** 每项问题的扣分
  （例如温度 ≥60°C 扣 25 分、SMART 总体判定 FAILED 扣 60 分）反映的是实践经验对严重程度的判断，
  不是经过校准的磁盘故障概率统计模型。请把这个评分当作分诊信号，而不是精确概率。
- **没有结构化（JSON/CSV）输出。** 只有人类可读日志。想跨多台机器聚合结果的场景，
  目前只能解析日志文本。
- **本脚本中的 `badblocks` 始终是只读模式（`-s`，不带 `-w`）。** 这是刻意的设计（见"目标与非目标"）
  ——不要在没有非常谨慎、单独加标志的前提下添加写模式选项，因为 `-w` 会摧毁目标磁盘上的数据。

## 如何扩展

- **结构化输出：** 增加 `--json` 或 `--csv` 参数，让每盘的评分/等级/问题数据以结构化格式
  与（或代替）人类可读日志一起输出，便于跨机器聚合。
- **更完整的 SAS 路径覆盖：** ATA 路径大约有 10 项评分属性；SAS 路径目前只对增长型缺陷表
  和读/写不可纠正错误计数评分。补齐 SAS 评分的对等程度会对多盘位机柜/企业级 SAS 部署有帮助。
- **NVMe 支持：** 目前 NVMe 默认不在扫描范围内（`--include-ssd` 会把它纳入枚举，
  但评分项是针对机械盘设计的，对 NVMe 不会产生有意义的评分）。需要一套独立的
  NVMe 专用检测路径（介质错误、已用百分比、来自 NVMe SMART 日志的温度阈值）及其自身的评分逻辑。
- **告警集成：** 退出码（0/1/2/3）已经适合搭配 cron + 通知封装脚本使用
  （例如只在非零退出码时触发的 MOTD/邮件/webhook 脚本）——这是刻意留给调用方做的，
  为了让脚本保持零依赖。
