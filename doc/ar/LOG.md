# hdd-health-check — السجل

يحفظ هذا السجل القرارات التاريخية المعتمدة والقيود المعروفة وسجل الإصدارات؛ دُمج v2.2.0 محليًا لكنه لم يُصدر.

## Multi-language

[English](../LOG.md) | [简体中文](../zh_cn/LOG.md) | [繁體中文](../zh_tw/LOG.md) | [繁體中文（香港）](../zh_hk/LOG.md) | [हिन्दी](../hi/LOG.md) | [Español](../es/LOG.md) | **العربية** | [Français](../fr/LOG.md)

## Documentation

- نظرة عامة على المشروع: [README](README.md)
- مبررات التصميم: [DESIGN](DESIGN.md)
- سجل الإصدارات: [LOG](LOG.md)
- قائمة مكونات الأطراف الثالثة: [THIRD_PARTY_NOTICES](THIRD_PARTY_NOTICES.md)

## Bugs

- [ ] لم يُتحقق من تكامل الأقراص HDD الفعلية أو سلوك صلاحيات الجذر/تثبيت الحزم أو systemd في هذا الدمج؛ كما أن فحوص الإصدار السابق v1.0.0 لم تشمل قرص HDD فعليًا ولا بيانات SMART (اقتصرت على الصياغة وخيار المساعدة). ورد أن النص البرمجي استُخدم قبل التوحيد الأصلي، لكن ذلك لا يغني عن اختبار مضبوط على الأجهزة.
- [ ] أوزان التقييم التقديرية ليست احتمالات فشل معايرة؛ وقد اختُبر تقييم SAS/SCSI بدرجة أقل من ATA، وقد يفشل تمرير أوامر SMART عبر USB/RAID. الإبلاغ عن درجات الحرارة المعتمد على الشركة المصنّعة غير مكتمل.
- [ ] لا توجد مخرجات منظمة بصيغة JSON/CSV. تُرفض بيانات الحالة الموجودة مسبقًا من v2.2 إذا لم تطابق التنسيق؛ غطّت الاختبارات المعزولة بمحاكاة اكتشاف حالة الخلفية والإيقاف الآمن مع `TMPDIR` مخصص وتقدم مسح السطح غير السليم؛ ولم يُتحقق بعد من سلوك systemd والأقراص الفعلية.
- [x] صحّح توحيد v1.0.0 تعليمات الاستنساخ التي كانت تشير إلى المسار غير الموجود `hdd-health-check-repo/main`، والأحرف الصينية التقليدية المختلطة في ملف README بالصينية المبسطة.

## Decisions

| القرارات | الأسباب |
| --- | --- |
| 2026-08-12: تقسيم المشروع المحلي إلى شجرة عمل Git ولقطات منفصلة؛ ورفض التخطيط المحلي المسطّح. | تطلبت لقطات الإصدارات التاريخية فصلها عن المصدر المتتبَّع. كانت تسمية `main/` المستخدمة آنذاك محلية فقط ولم تكن قط جزءًا من نسخة GitHub؛ وكان التخطيط لاستخدام الوسوم ولقطات `git archive`. |
| 2026-08-13: إعادة تسمية `main/` المحلي إلى `repo/`؛ وإصلاح مسارات التثبيت والترويسات ثنائية اللغة و`.gitignore`؛ واستبعاد مسارات النسخة الخاصة واللقطات من الحالة المنشورة. ورفض إدراج الوثائق المرحّلة القديمة في التزام دون تغيير. | تضع نسخة GitHub النص البرمجي في جذرها. كانت الأمثلة القديمة ستفشل فور الاستنساخ؛ ولم يكن لمسارات محلية وأخطاء ترجمة مكان في الوثائق العامة. |
| 2026-08-13: عدم محاكاة فحص إصدار على قرص HDD فعلي باستخدام القرص الافتراضي المتاح؛ والإفصاح عن محدودية التحقق. | لم تتوفر أجهزة SMART صالحة ولا `smartmontools`؛ ولم يكن تثبيت الحزم لمسح قرص افتراضي سيختبر منطق HDD. |
| 2026-08-13: استبدال سجل v1.0.0 العام بالتزام واحد نظيف بهوية بريد noreply ووسم مشروح، مع حفظ السجل الأصلي في أرشيف خاص أعيدت تسميته؛ ورفض الدفع القسري إلى المستودع العام السابق أو إبقاء التزامه الوسيط الذي حل محله الالتزام الجديد. | احتوى الالتزام العام السابق على بريد شخصي في بيانات Git الوصفية. لا تنتقل النسخ المستنسخة/المتفرعة القائمة تلقائيًا، ولا يمكن ضمان محو التعرض السابق من ذاكرات التخزين المؤقت. هذا سجل تاريخي، وليس إذنًا بإعادة كتابة السجل مرة أخرى. |
| الدمج الحالي: اعتماد سلوك v2.2.0 المقدم دون ضمانات توافق مع v1، والإبقاء على ترخيص MIT القائم. | يضيف النص البرمجي الجديد حالة دائمة ومهام خلفية اختيارية؛ ويُتجاهل `-w`. هذا الدمج ليس ادعاءً بوجود إصدار أو وسم أو تحقق على الأجهزة. |

## Changelog

### غير مُصدر — دمج v2.2.0 (بلا تاريخ إصدار)

#### Added

- نتائج دائمة لكل قرص، وسجل عدادات SMART، وسجلات الإصلاح والتقارير؛ وقائمة تفاعلية، وقياس سرعة القراءة بأخذ عينات، ومسح قابل للاستئناف لزمن تأخر قراءة السطح مع إعادة فحص موجّهة للكتل التالفة، وفحص كامل للقرص باستخدام `badblocks` للقراءة فقط، واختبارات تحمل لقراءة الواجهة، وتنفيذ اختياري في الخلفية عبر وحدات systemd مؤقتة.

#### Changed

- فحوص سريعة افتراضية في الوضع الدفعي، ونتائج محددة الصلاحية قابلة لإعادة الاستخدام، و`--rescan`؛ ويختار `-r/--run` الوحدات، ويدير `--status`/`--stop` النسخة الجارية. تتطابق `-t short|long` و`-s` و`-b` مع الوحدات؛ ويُتجاهل `-w/--wait` بدل الانتظار. السلوك الجديد ليس توافقًا مع CLI الخاص بـ v1.
- تُكتب حالة المضيف والسجلات كلٌّ على حدة؛ ويقيّد دمج v2.2.0 المقدم تحليل الحالة والخطط والتعامل مع المسارات/الأجهزة. لم يُجر هنا اختبار تكامل على قرص HDD فعلي أو بصلاحيات الجذر أو مع systemd.

### v1.0.0 — 2026-08-08

#### Added

- الفحص الأولي لحالة HDD عبر 12 جانبًا: بيانات الجهاز/الواجهة، وإمكانية SMART وحكمها العام، وسمات ATA أو عدادات عيوب/أخطاء SAS، وسجلات الأخطاء والاختبارات الذاتية، وبدء الاختبارات الذاتية القصيرة/الطويلة، والعمر المتوقع/الحمل، وأخطاء إدخال/إخراج النواة، وكشف التركيب وحالة القراءة فقط، واختبار أداء اختياري للقراءة فقط باستخدام `hdparm` ومسح باستخدام `badblocks`.
- درجة تقديرية من 0 إلى 100 وأربع فئات مع رموز خروج العملية 0/1/2/3، وكشف تلقائي لتمرير أوامر SMART، واختيار الأقراص تفاعليًا أو دفعيًا، وعرض تثبيت `smartmontools` المفقود باستخدام `apt`.
- تظل وثائق التصميم والاستخدام الكاملة للإصدار v1.0.0 محفوظة في وسم Git ‏`v1.0.0` (مثل `git show v1.0.0:DESIGN.md` و`git show v1.0.0:README.zh.md`)؛ أوامر v1 القديمة ليست إرشادات للإصدار الحالي.
